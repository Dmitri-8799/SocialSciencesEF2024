<?xml version="1.0" encoding="utf-8" ?>

<toolkit:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SocialSciencesEF2024.PopUp.PopupIfRightPage"
             xmlns:toolkit = "http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:viewmodels="clr-namespace:SocialSciencesEF2024.ViewModels"
             x:DataType="viewmodels:TableRb3ViewModel">

     <Frame>
  <VerticalStackLayout>
        <Label 
            Text="{Binding CurrentQuestion.IfRight}"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />

          <Button Text="ОК" Command="{Binding ButtonNextQuestionPopup}" IsEnabled="True" >

           </Button>

    </VerticalStackLayout>

    </Frame>

</toolkit:Popup>
